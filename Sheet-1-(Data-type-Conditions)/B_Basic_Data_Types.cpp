/*

The hardest battles are the ones you fight in silence — when no one sees your effort,

no one claps for you, and yet you keep going because quitting

would hurt more than pain itself.

I Say to you keep going and STAY HARD i proud of you.

*/

#include <iostream>
using namespace std;

#define ll  long long
#define ld  long double
#define ull unsigned long long int

int main() {

    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int number1; cin >> number1;
    ll number2; cin >> number2;
    char item; cin >> item;
    float number3; cin >> number3;
    double number4; cin >> number4;

    cout << number1 << '\n' << number2 << '\n' << item << '\n' << number3 << '\n' << number4; 

    return 0;
}
